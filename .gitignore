# --- Python / runtime ---
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env

# --- OS ---
.DS_Store

# district.sql and "state 1.sql" (the one-time boundary dumps loaded by
# scripts/setup_database.sh) are committed via Git LFS — see .gitattributes —
# instead of being excluded, since they're needed for a from-scratch clone.

# --- Orphaned local cache from the old, unused grid_generator.py path ---
# The live app fetches grid cells from PostGIS via db.py instead; these files
# belong to an earlier local-file-cache implementation that nothing calls.
/data/grids/
/data/districts.geojson
/data/state.geojson

# --- Runtime state written while the app runs ---
# Accidents + facilities live only under latest data/. Reach/scorecard JSON is
# regenerated by scripts/recompute_reach.py. Uploaded overrides + OSRM caches
# should not show up as local git changes.
/data/grid_reach/
/data/rbg_grids/
/data/accidents_active.csv
/data/dataset_state.json
/data/accident_*_safety*.json
/data/district_scorecard*.json
/data/recompute.log
/data/recompute.pid
/dashboard/cache/

# --- Self-hosted OSRM (Haryana OSM extract + processed graph) ---
/osrm-data/

# --- Superseded 3D-readiness feature ---
# That view was removed from the app; this is its leftover output/docs.
/dashboard/static/scored/
/dashboard/templates/_nav.html
/dashboard/grid_generator.py
/scripts/score_engine.py
/scripts/export_geojson.py
/READINESS_README.md
/SCORING_SPEC.md
/view_3d.command
/data/coverage_demo_backups/

# Standalone analytics deliverable — large, regenerable, not part of the app
hospital-grid proximity */
hospital_grid_coverage_*.csv*

# --- Local scratch, backups and handover bundles (not part of the app) ---
# These are working folders on the dev machine: old copies kept "just in case",
# zipped handover bundles, deck screenshots and one-off analysis exports. They
# are large (_handoff alone is ~156 MB) and regenerable, so they stay local.
/Archived/
/_handoff/
/_deckimg/
/_backup_20260831/
/_to_delete/
/_sev_tmp.json
/Untitled
/data/_backup_pre_regrid_20260828/
/data/rbg_probe/
/data/regrid_*.log
/faridabad_ambulance_old_vs_new.csv
